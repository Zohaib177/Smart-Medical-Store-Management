const express=require('express');const auth=require('../middleware/authMiddleware');const validate=require('../middleware/validateRequest');const c=require('../controllers/saleController');const v=require('../validators/saleValidators');const r=express.Router();r.use(auth);r.get('/summary',c.summary);r.get('/options',c.options);r.get('/medicines',v.pos,validate,c.medicines);r.get('/',v.list,validate,c.list);r.get('/:id',v.id,validate,c.detail);r.post('/',v.create,validate,c.create);r.patch('/:id/cancel',v.id,v.cancel,validate,c.cancel);module.exports=r;
