const express=require('express');const auth=require('../middleware/authMiddleware');const validate=require('../middleware/validateRequest');const c=require('../controllers/purchaseController');const v=require('../validators/purchaseValidators');const r=express.Router();r.use(auth);r.get('/',v.listPurchasesValidator,validate,c.listPurchases);r.get('/summary',c.getPurchaseSummary);r.get('/options',c.getPurchaseOptions);r.get('/:id',v.purchaseIdValidator,validate,c.getPurchase);r.post('/',v.createPurchaseValidator,validate,c.createPurchase);r.patch('/:id/cancel',v.purchaseIdValidator,v.cancelPurchaseValidator,validate,c.cancelPurchase);module.exports=r;
