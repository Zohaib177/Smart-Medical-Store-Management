import{formatCurrency}from'../../utils/formatters';export default function PosTotals({totals}){return <dl aria-live="polite" className="space-y-2 rounded-2xl bg-slate-950 p-4 text-sm text-white">{[['Subtotal',totals.subtotal],['Discount',-totals.discount],['Tax',totals.tax],['Total',totals.total],['Paid',totals.paid],['Due',totals.due],['Change',totals.change]].map(([l,v],i)=><div key={l} className={`flex justify-between ${i===3?'border-t border-white/20 pt-2 text-base font-black':'text-slate-300'}`}><dt>{l}</dt><dd>{formatCurrency(v)}</dd></div>)}<div className="flex justify-between border-t border-white/20 pt-2"><dt>Payment Status</dt><dd className="font-bold capitalize">{totals.paymentStatus}</dd></div></dl>}
