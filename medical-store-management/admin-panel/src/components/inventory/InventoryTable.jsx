import { Boxes } from 'lucide-react'; import EmptyState from '../ui/EmptyState'; import InventoryTableRow from './InventoryTableRow';
export default function InventoryTable({ items, isLoading, hasFilters, onAdjust, onHistory }) { if(isLoading)return <div className="p-5"><div className="h-72 animate-pulse rounded-xl bg-slate-100" aria-label="Loading inventory"/></div>; if(!items.length)return <div className="p-5"><EmptyState icon={Boxes} title={hasFilters?'No matching inventory':'No medicines in inventory'} description={hasFilters?'Try clearing or changing the filters.':'Add medicines from Medicine Management to begin tracking stock.'}/></div>; return <div className="overflow-x-auto"><table className="min-w-[1300px] w-full text-left"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{['Medicine','Category','Company','Batch','Purchase Price','Current Stock','Minimum Stock','Stock Status','Expiry','Inventory Value','Actions'].map(x=><th key={x} scope="col" className="px-4 py-3 font-semibold last:text-right">{x}</th>)}</tr></thead><tbody>{items.map(item=><InventoryTableRow key={item.medicineId} medicine={item} onAdjust={onAdjust} onHistory={onHistory}/>)}</tbody></table></div>; }
