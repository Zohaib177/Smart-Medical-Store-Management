import { AlertTriangle, Boxes, CircleDollarSign, Clock3, PackageCheck, PackageX, Pill, ShieldAlert } from 'lucide-react';
import LoadingCard from '../ui/LoadingCard'; import { formatCurrency, formatNumber } from '../../utils/formatters';
const cards = [
  ['totalMedicines', 'Total Medicines', Pill, 'All catalog medicines'], ['totalStockUnits', 'Total Stock Units', Boxes, 'Units currently recorded'], ['inStockMedicines', 'In Stock', PackageCheck, 'Above minimum level'], ['lowStockMedicines', 'Low Stock', AlertTriangle, 'At or below minimum'], ['outOfStockMedicines', 'Out of Stock', PackageX, 'No units available'], ['expiredMedicines', 'Expired', ShieldAlert, 'Past expiry date'], ['expiringSoonMedicines', 'Expiring Soon', Clock3, 'Within the next 30 days'], ['totalInventoryValue', 'Inventory Cost Value', CircleDollarSign, 'Based on purchase price'],
];
export default function InventorySummaryCards({ summary, isLoading }) { if (isLoading) return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([key]) => <LoadingCard key={key} />)}</div>; return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([key, label, Icon, description]) => <article key={key} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><p className="text-sm font-semibold text-slate-600">{label}</p><span className="rounded-xl bg-emerald-50 p-2.5 text-emerald-700"><Icon className="h-5 w-5" /></span></div><p className="mt-4 text-2xl font-bold text-slate-900">{key === 'totalInventoryValue' ? formatCurrency(summary?.[key]) : formatNumber(summary?.[key])}</p><p className="mt-1 text-xs text-slate-500">{description}</p></article>)}</div>; }
