import { RotateCcw } from 'lucide-react'; import Button from '../ui/Button'; import SearchInput from '../ui/SearchInput'; import SelectInput from '../ui/SelectInput';
const types=[{value:'',label:'All transaction types'},{value:'stockIn',label:'Stock In'},{value:'stockOut',label:'Stock Out'},{value:'correction',label:'Correction'},{value:'purchase',label:'Purchase'},{value:'purchaseCancellation',label:'Purchase Cancellation'},{value:'sale',label:'Sale'},{value:'saleCancellation',label:'Sale Cancellation'}]; const sorts=[{value:'desc',label:'Newest first'},{value:'asc',label:'Oldest first'}];
export default function InventoryHistoryFilters({ filters, medicines, onChange, onClear }) { const medicineOptions=[{value:'',label:'All medicines'},...medicines.map(x=>({value:String(x.medicineId),label:x.medicineName}))]; const invalid=filters.dateFrom&&filters.dateTo&&filters.dateFrom>filters.dateTo; const changed=filters.search||filters.medicineId||filters.transactionType||filters.dateFrom||filters.dateTo||filters.sortDirection!=='desc'; return <div className="space-y-3 border-b border-slate-200 p-4"><SearchInput value={filters.search} onChange={v=>onChange('search',v)} placeholder="Search medicine, reason, notes or admin..." label="Search inventory history"/><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><SelectInput label="Medicine" value={filters.medicineId} onChange={v=>onChange('medicineId',v)} options={medicineOptions}/><SelectInput label="Transaction type" value={filters.transactionType} onChange={v=>onChange('transactionType',v)} options={types}/><label className="text-xs font-semibold text-slate-600">Date from<input type="date" value={filters.dateFrom} max={filters.dateTo||undefined} onChange={e=>onChange('dateFrom',e.target.value)} className="mt-1 h-10 w-full rounded-xl border border-slate-200 px-3 text-sm focus:border-emerald-500 focus:outline-none focus:ring-2 focus:ring-emerald-100"/></label><label className="text-xs font-semibold text-slate-600">Date to<input type="date" value={filters.dateTo} min={filters.dateFrom||undefined} onChange={e=>onChange('dateTo',e.target.value)} className="mt-1 h-10 w-full rounded-xl border border-slate-200 px-3 text-sm focus:border-emerald-500 focus:outline-none focus:ring-2 focus:ring-emerald-100"/></label><SelectInput label="Sort direction" value={filters.sortDirection} onChange={v=>onChange('sortDirection',v)} options={sorts}/></div>{invalid&&<p role="alert" className="text-sm font-medium text-red-600">Date from cannot be after date to.</p>}<Button variant="ghost" size="sm" onClick={onClear} disabled={!changed}><RotateCcw className="h-4 w-4"/> Clear filters</Button></div>; }
