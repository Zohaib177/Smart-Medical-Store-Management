import Modal from '../ui/Modal'; import Pagination from '../ui/Pagination'; import ErrorState from '../ui/ErrorState'; import EmptyState from '../ui/EmptyState'; import StockStatusBadge from '../medicines/StockStatusBadge'; import InventoryHistoryTable from './InventoryHistoryTable';
export default function MedicineHistoryModal({ isOpen, onClose, result, filters, pagination, isLoading, error, onFilterChange, onPageChange, onRetry }) { const medicine=result?.medicine; const invalid=filters.dateFrom&&filters.dateTo&&filters.dateFrom>filters.dateTo; return <Modal isOpen={isOpen} onClose={onClose} title={`${medicine?.medicineName||'Medicine'} Stock History`} description="Paginated audit history for this medicine." size="lg"><div className="space-y-4 p-5 sm:p-6">{medicine&&<div className="flex flex-wrap items-center justify-between gap-3 rounded-2xl bg-slate-50 p-4"><div><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">Current stock</p><p className="text-xl font-bold text-slate-900">{medicine.stockQuantity} units</p><p className="text-xs text-slate-500">Minimum: {medicine.minimumStock}</p></div><StockStatusBadge status={medicine.stockStatus}/></div>}<div className="grid gap-3 sm:grid-cols-3"><select aria-label="Transaction type" value={filters.transactionType} onChange={e=>onFilterChange('transactionType',e.target.value)} className="h-10 rounded-xl border border-slate-200 px-3 text-sm"><option value="">All transaction types</option><option value="stockIn">Stock In</option><option value="stockOut">Stock Out</option><option value="correction">Correction</option></select><label className="text-xs font-semibold text-slate-600">From<input type="date" value={filters.dateFrom} max={filters.dateTo||undefined} onChange={e=>onFilterChange('dateFrom',e.target.value)} className="mt-1 h-10 w-full rounded-xl border border-slate-200 px-3 text-sm"/></label><label className="text-xs font-semibold text-slate-600">To<input type="date" value={filters.dateTo} min={filters.dateFrom||undefined} onChange={e=>onFilterChange('dateTo',e.target.value)} className="mt-1 h-10 w-full rounded-xl border border-slate-200 px-3 text-sm"/></label></div>{invalid&&<p className="text-sm font-medium text-red-600">Date from cannot be after date to.</p>}{error?<ErrorState title="Unable to load medicine history" description="Try this request again." onRetry={onRetry}/>:result?.history?.length||isLoading?<div className="-mx-5 overflow-hidden border-y border-slate-200 sm:-mx-6"><InventoryHistoryTable items={result?.history||[]} isLoading={isLoading} onMedicineHistory={()=>{}}/><Pagination pagination={pagination} onPageChange={onPageChange}/></div>:<EmptyState title="No stock history" description="No adjustments match the selected filters."/>}</div></Modal>; }
