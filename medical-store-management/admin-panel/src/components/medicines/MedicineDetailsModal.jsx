import { formatCurrency, formatDateTime, formatDate } from '../../utils/formatters'; import { getDaysUntilExpiryText } from '../../utils/medicineHelpers'; import LoadingCard from '../ui/LoadingCard'; import Modal from '../ui/Modal'; import ExpiryStatusBadge from './ExpiryStatusBadge'; import MedicineImagePreview from './MedicineImagePreview'; import MedicineStatusBadge from './MedicineStatusBadge'; import StockStatusBadge from './StockStatusBadge';
export default function MedicineDetailsModal({ medicine, isOpen, onClose, isLoading }) { const value = (x) => x === null || x === undefined || x === '' ? 'Not provided' : x; return <Modal isOpen={isOpen} onClose={onClose} title="Medicine Details" description="Pricing, stock, product and expiry information." size="lg"><div className="px-5 py-5 sm:px-6">{isLoading ? <LoadingCard /> : medicine && <div className="space-y-6"><div className="flex items-start gap-4 rounded-2xl bg-slate-50 p-4"><MedicineImagePreview src={medicine.imageUrl} alt={medicine.medicineName} className="h-20 w-20" /><div><h3 className="text-xl font-bold text-slate-900">{medicine.medicineName}</h3><p className="mt-1 text-sm text-slate-500">{value(medicine.genericName)}{medicine.strength ? ` · ${medicine.strength}` : ''}</p><div className="mt-2 flex flex-wrap gap-2"><MedicineStatusBadge status={medicine.status} /><StockStatusBadge status={medicine.stockStatus} /><ExpiryStatusBadge status={medicine.expiryStatus} /></div></div></div><Group title="Classification"><Details items={[['Category',medicine.category.categoryName],['Company',medicine.company.companyName],['Dosage form',value(medicine.dosageForm)],['Strength',value(medicine.strength)]]} /></Group><Group title="Product information"><Details items={[['Batch number',value(medicine.batchNumber)],['Barcode',value(medicine.barcode)],['Prescription required',medicine.prescriptionRequired?'Yes':'No'],['Image URL',value(medicine.imageUrl)]]} /><Detail label="Description" value={value(medicine.description)} /></Group><Group title="Pricing and stock"><Details items={[['Purchase price',formatCurrency(medicine.purchasePrice)],['Sale price',formatCurrency(medicine.salePrice)],['Stock quantity',medicine.stockQuantity],['Minimum stock',medicine.minimumStock]]} /></Group><Group title="Dates and expiry"><Details items={[['Manufacturing date',formatDate(medicine.manufacturingDate)],['Expiry date',formatDate(medicine.expiryDate)],['Expiry timeline',getDaysUntilExpiryText(medicine.daysUntilExpiry)],['Created',formatDateTime(medicine.createdAt)],['Last updated',formatDateTime(medicine.updatedAt)]]} /></Group></div>}</div></Modal>; }
function Group({ title, children }) { return <section><h4 className="mb-3 text-sm font-bold uppercase tracking-wider text-slate-500">{title}</h4>{children}</section>; } function Details({ items }) { return <div className="grid gap-3 sm:grid-cols-2">{items.map(([label,value]) => <Detail key={label} label={label} value={value} />)}</div>; } function Detail({ label, value }) { return <div className="min-w-0 rounded-xl border border-slate-200 p-3"><p className="text-xs font-semibold text-slate-500">{label}</p><p className="mt-1 break-words text-sm text-slate-800">{value}</p></div>; }
