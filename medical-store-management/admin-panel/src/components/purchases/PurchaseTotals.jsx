import{formatCurrency}from'../../utils/formatters';import PaymentStatusBadge from'./PaymentStatusBadge';export default function PurchaseTotals({totals}){return <aside className="rounded-[22px] bg-gradient-to-br from-emerald-950 to-teal-800 p-5 text-white shadow-lg"><h2 className="text-lg font-bold">Payment Summary</h2><div className="mt-5 space-y-3 text-sm">{[['Subtotal',totals.subtotal],['Discount',-totals.discount],['Tax',totals.tax],['Total',totals.total],['Paid',totals.paid],['Due',totals.due]].map(([l,v])=><div key={l} className={`flex justify-between ${l==='Total'||l==='Due'?'border-t border-white/15 pt-3 font-bold':''}`}><span className="text-emerald-100/80">{l}</span><span>{formatCurrency(v)}</span></div>)}</div><div className="mt-5 rounded-xl bg-white p-3 text-slate-900"><span className="mr-2 text-xs font-bold uppercase text-slate-500">Status</span><PaymentStatusBadge status={totals.paymentStatus}/></div></aside>}
