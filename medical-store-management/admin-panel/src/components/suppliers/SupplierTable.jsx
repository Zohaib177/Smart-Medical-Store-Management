import { Truck } from 'lucide-react';import EmptyState from '../ui/EmptyState';import LoadingCard from '../ui/LoadingCard';import SupplierTableRow from './SupplierTableRow';
export default function SupplierTable({suppliers,isLoading,hasFilters,actions}){if(isLoading)return <div className="grid gap-3 p-4 sm:grid-cols-2 lg:grid-cols-3">{Array.from({length:6},(_,i)=><LoadingCard key={i}/>)}</div>;if(!suppliers.length)return <div className="p-5"><EmptyState icon={Truck} title={hasFilters?'No matching suppliers':'No suppliers yet'} description={hasFilters?'Try changing or clearing the filters.':'Add the first medicine supplier to begin.'}/></div>;return <div className="max-w-full overflow-x-auto"><table className="w-full min-w-[1200px] text-left"><thead className="bg-slate-50 text-xs font-semibold uppercase tracking-wider text-slate-500"><tr>{['Supplier','Contact Person','Phone','Email','City','Purchases','Status','Created','Actions'].map(x=><th key={x} className={`px-4 py-3 ${x==='Purchases'?'text-center':''} ${x==='Actions'?'text-right':''}`}>{x}</th>)}</tr></thead><tbody>{suppliers.map(s=><SupplierTableRow key={s.id} supplier={s} {...actions}/>)}</tbody></table></div>;}
