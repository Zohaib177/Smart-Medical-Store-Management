import { RotateCcw } from 'lucide-react';import Button from '../ui/Button';import SearchInput from '../ui/SearchInput';import SelectInput from '../ui/SelectInput';
const statuses=[{value:'',label:'All statuses'},{value:'active',label:'Active'},{value:'inactive',label:'Inactive'}];const sorts=[{value:'created_at:desc',label:'Newest'},{value:'created_at:asc',label:'Oldest'},{value:'supplier_name:asc',label:'Name A–Z'},{value:'supplier_name:desc',label:'Name Z–A'},{value:'updated_at:desc',label:'Recently updated'},{value:'purchase_count:desc',label:'Highest purchase count'},{value:'purchase_count:asc',label:'Lowest purchase count'}];
export default function SupplierFilters({filters,cities,onSearch,onStatus,onCity,onSort,onClear}){const cityOptions=[{value:'',label:'All cities'},...cities.map(city=>({value:city,label:city}))];const changed=filters.search||filters.status||filters.city||`${filters.sortBy}:${filters.sortDirection}`!=='created_at:desc';return <div className="space-y-3 border-b border-slate-200 p-4"><SearchInput value={filters.search} onChange={onSearch} placeholder="Search supplier, contact, email, phone, city..." label="Search suppliers"/><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><SelectInput label="Status" value={filters.status} onChange={onStatus} options={statuses}/><SelectInput label="City" value={filters.city} onChange={onCity} options={cityOptions}/><SelectInput label="Sort suppliers" value={`${filters.sortBy}:${filters.sortDirection}`} onChange={onSort} options={sorts}/><Button variant="ghost" size="sm" onClick={onClear} disabled={!changed}><RotateCcw className="h-4 w-4"/> Clear filters</Button></div></div>;}
