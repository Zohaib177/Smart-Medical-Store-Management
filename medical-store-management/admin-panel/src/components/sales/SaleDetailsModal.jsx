import{Ban,LoaderCircle,Printer}from'lucide-react';import Modal from'../ui/Modal';import Button from'../ui/Button';import SaleReceipt from'./SaleReceipt';export default function SaleDetailsModal({sale,open,loading,onClose,onCancel}){return <Modal isOpen={open} onClose={onClose} title="Sale Details" description="Invoice, medicine and payment information." size="lg">{loading?<div className="flex min-h-72 items-center justify-center text-slate-500"><LoaderCircle className="mr-2 h-5 w-5 animate-spin"/> Loading sale...</div>:<SaleReceipt sale={sale}/>} {!loading&&sale&&<div className="no-print sticky bottom-0 flex flex-wrap justify-end gap-3 border-t bg-white/95 p-5"><Button variant="secondary" onClick={onClose}>Close</Button><Button variant="secondary" onClick={()=>window.print()}><Printer className="h-4 w-4"/> Print Receipt</Button><Button variant="danger" disabled={sale.saleStatus==='cancelled'} onClick={()=>onCancel(sale)}><Ban className="h-4 w-4"/> Cancel Sale</Button></div>}</Modal>}
