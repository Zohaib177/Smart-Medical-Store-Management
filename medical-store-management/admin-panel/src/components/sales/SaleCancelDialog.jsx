import{useEffect,useState}from'react';import{AlertTriangle,LoaderCircle}from'lucide-react';import Modal from'../ui/Modal';import Button from'../ui/Button';export default function SaleCancelDialog({sale,open,loading,onClose,onConfirm}){const[reason,setReason]=useState(''),[error,setError]=useState('');useEffect(()=>{if(open){setReason('');setError('')}},[open]);const submit=async e=>{e.preventDefault();if(reason.trim().length<3){setError('Enter at least 3 characters.');return}try{await onConfirm(reason.trim())}catch{}};return <Modal isOpen={open} onClose={onClose} title={`Cancel sale "${sale?.saleNumber||''}"?`} description="This is an audited stock reversal, not a refund or deletion." size="sm"><form onSubmit={submit} className="space-y-5 p-5"><div className="flex gap-3 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900"><AlertTriangle className="h-5 w-5 shrink-0"/> Sold stock will be restored, inventory history will record the reversal, and this sale will remain stored.</div><label className="block text-sm font-bold">Cancellation Reason<textarea value={reason} onChange={e=>{setReason(e.target.value);setError('')}} rows="4" maxLength="500" className="mt-2 w-full rounded-xl border border-slate-200 p-3 outline-none focus:border-red-500 focus:ring-2 focus:ring-red-100"/>{error&&<span className="mt-1 block text-xs text-red-600">{error}</span>}</label><div className="flex justify-end gap-3"><Button variant="secondary" onClick={onClose} disabled={loading}>Keep Sale</Button><Button type="submit" variant="danger" disabled={loading}>{loading&&<LoaderCircle className="h-4 w-4 animate-spin"/>}{loading?'Cancelling...':'Cancel Sale'}</Button></div></form></Modal>}
